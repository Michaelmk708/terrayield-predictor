# TERRAYIELD — Environment Variables
# Copy this to .env.local for local development (never commit .env.local)

# Point this at your Django REST Framework backend
VITE_API_URL=http://localhost:8000/api
